core features:
// run seeders and migrations before using the app.

1- Admin can perform crud operations on students

2- Admin can search for students

3- Admin can see all messages that are bein created

4- With permissions, users can send messages to teachers.

5- admin can edit roles, create permissions and relate it to a role, assign role to users.